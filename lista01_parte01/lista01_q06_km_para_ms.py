velocidade_km = float(input('Digite uma velocidade em km/h: '))

velocidade_ms = velocidade_km / 3.6

print('A velocidade {} km/h é equivalente a: {:.2f} m/s'.format(velocidade_km, velocidade_ms))
