base = float(input('Digite o valor da base do retangulo: '))
altura = float(input('Digite o valor da altura do retangulo: '))

area = base * altura

print('A area desse retangulo corresponde á: {}'.format(area))